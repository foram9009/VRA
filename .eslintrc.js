/** @type {import('eslint').Linter.Config} */
module.exports = {
  root: true,
  extends: [
    'next/core-web-vitals',
    'next/typescript',
  ],
  rules: {
    // Allow explicit any with a suppression comment — warn, don't error
    '@typescript-eslint/no-explicit-any': 'warn',
    // Enforce consistent import types
    '@typescript-eslint/consistent-type-imports': [
      'warn',
      { prefer: 'type-imports', fixStyle: 'inline-type-imports' },
    ],
    // Prevent React being flagged as unused (Next.js auto-imports it)
    'react/react-in-jsx-scope': 'off',
    // Allow async server action functions that may not always await
    '@typescript-eslint/require-await': 'off',
    // Useful but can be noisy — warn only (allow warn and error)
    'no-console': ['warn', { allow: ['warn', 'error'] }],
    // Next.js Image is preferred over <img> — warn not error so admin UI img tags don't block build
    '@next/next/no-img-element': 'warn',
    // Allow require() in config files like tailwind.config.ts
    '@typescript-eslint/no-require-imports': 'off',
    // Allow unused vars prefixed with _ (conventional ignore pattern)
    '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }],
  },
  ignorePatterns: [
    'node_modules/',
    '.next/',
    'out/',
    'prisma/generated/',
    'next-env.d.ts', // Auto-generated by Next.js — has triple-slash references by design
  ],
};
